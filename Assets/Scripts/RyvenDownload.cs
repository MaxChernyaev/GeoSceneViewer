using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Net;
using System.IO;

public class RyvenDownload : MonoBehaviour
{
    // WebClient webClient = new WebClient();

    // // Start is called before the first frame update
    // void Start()
    // {
        
    // }

    // // Update is called once per frame
    // void Update()
    // {
        
    // }

    // public void DownloadScene()
    // {
    //     string WebServerIP = GameObject.Find("Main Camera").GetComponent<ARSceneMakingManager>().WebServerIP;
    //     string RelativePath = GameObject.Find("Main Camera").GetComponent<ARSceneMakingManager>().RelativePath;
    //     Directory.CreateDirectory(RelativePath + "\\RyvenScene");
    //     webClient.DownloadFile(WebServerIP + "/scene.json", RelativePath + "\\RyvenScene" + "\\scene.json"); // скачиваю новый scene.json
        
    // }
}
